const buttonCheck = getElementsById(button_add_collab)

buttonCheck.addEventListener('click', event => {
  // buttonCheck.classList.add('check');
  // buttonCheck.innerHTML(<i class="fas fa-check-circle"></i>)
  buttonCheck.classList.add("disabled");
  buttonCheck.innerText = `<i class="fas fa-check-circle"></i>`;
  // buttonCheck.innerText = "";
  // buttonCheck.innerHTML(`<i class="fas fa-check-circle"></i>`);
});
