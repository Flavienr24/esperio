const displayCheckOnButtonCollabClick = () => {
  // TODO: Select the big green button
  const button = document.querySelector(".JS-toggle");
  console.log(button)
  // TODO: Bind the `click` event to the button
  // TODO: On click, display `Thank you!` in a JavaScript alert!
  button.addEventListener('click', (event) => {
    button.classList.add("disabled");
    button.innertext = `<i class="fas fa-check"></i>`;
  });
};

displayCheckOnButtonCollabClick();
