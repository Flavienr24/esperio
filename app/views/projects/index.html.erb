<h1> List of projects</h1>

<!--  Description Project -->
<div>
    <% @projects.each do |project| %>
    <div class="row">
      <div class="col-12 col-md-6 d-flex">
      <h3> <%= project.name %></h3>
      <p>  starting date: <%= project.created_at%> </p>
      <p> statute: <%= project.status %> </p>
      <p><%= project.description %> </p>
 <!--      <p> <%= project.open_to_apply %></p>
      <p> project.visibility</p> -->
      </div>
    </div>
        <div class="row">
        <img src="${@user.avatar" alt="avatar">
        <%= link_to "More ", project_path(project), class: 'btn btn-primary' %>
      </div>
    <% end %>
</div>

<%= link_to 'Create Project', new_project_path(@project), class: 'btn btn-primary'%>
<!-- Projet Expertise -->
  <h2>Expertises</h2>
  <%= @myexpertises %>

