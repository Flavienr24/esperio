<div class="signup-wrapper container">
  <div class="flat-card">
    <div class="px-5 mt-5">
      <div class="d-flex justify-content-center pt-4 mb-2"><h3>New user profile</h3></div>

<!-- Scrapping RS form -->
      <h4 class="mt-4 mb-4">Do you have a ResearchGate account ?</h4>
      <%= form_tag user_get_researchgate_info_path, method: :post do %>
        <input type="text" name="username" id="username" placeholder:="ResearchGate username account" required>
        <button name="public"  type="submit" class="btn btn-primary" data-toggle="button">ReseachGate</button>
      <% end %>
<!-- Scrapping RS form -->

        <h4 class="mt-4 mb-4">Complete your profile</h4>
         <!-- <#%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %> -->
        <%= simple_form_for @user do |f| %>
        <%= f.error_notification %>
      <div class="form-inputs">
        <%= f.text_field :fullname,
        required: true,
        autofocus: true,
        value: session[:first_name]&.concat(" " + session[:last_name]),
        placeholder: "John Lascience",
        input_html: { autocomplete: "fullname" } %>
        <%= f.text_field :skill_list,
        required: true,
        autofocus: true,
        value: session[:skills],
        placeholder: "Skills",
        input_html: { autocomplete: "skills"} %>
        <%= f.input :country, as: :string,
        required: true,
        autofocus: true,
        placeholder: "France",
        input_html: { autocomplete: "country" }%>
        <%= f.input :city,
        required: true,
        autofocus: true,
        placeholder: "Paris",
        input_html: { autocomplete: "city" }%>
        <%= f.input :function,
        required: true,
        autofocus: true,
        placeholder: "Fellow Researcher",
        input_html: { autocomplete: "function" }%>
        <%= f.input :school,
        required: true,
        autofocus: true,
        placeholder: "Ecoles des Ponts, Paristech",
        input_html: { autocomplete: "school" }%>
        <%= f.input :phone,
        required: true,
        autofocus: true,
        placeholder: "+33 7 43 56 55 77",
        input_html: { autocomplete: "phone" }%>
        <%= f.association :company, as: :select,
        placeholder: "Feellows" %>
        <h4 class="mt-4 mb-4">Your email and password</h4>
        <%= f.input :email,
        required: true,
        autofocus: true,
        placeholder: "john.lascience@feellows.com",
        input_html: { autocomplete: "email" }%>
        <%= f.input :password,
        required: true,
        placeholder: "•••••••••••",
        hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
        input_html: { autocomplete: "new-password" } %>
        <%= f.input :password_confirmation,
        required: true,
        placeholder: "•••••••••••",
        input_html: { autocomplete: "new-password" } %>
      </div>

      <div class="d-flex pb-3 form-actions align-items-center justify-content-between">
        <%= link_to "Back", new_session_path(resource_name), class:"btn btn-secondary" %>
        <%= f.button :submit, "Sign up", class:"btn btn-primary"%>
      </div>
      <% end %>
      <%# render "devise/shared/links" %>
    </div>
  </div>
</div>
